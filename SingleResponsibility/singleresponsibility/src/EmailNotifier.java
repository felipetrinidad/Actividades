// Responsabilidad: notificar por email
public class EmailNotifier {
    public void sendEmail(String to, String message) {
        System.out.println("\nEnviando email a: " + to + ". Mensaje: " + message);
    }
}
